plugins {
    id "com.moowork.node" version "1.3.1"
}

task yarnBuild(type: YarnTask) {
    args = ['build']
}

task yarnStart(type: YarnTask) {
    args = ['start']
}

task yarnStop(type: YarnTask) {
    args = ['stop']
}